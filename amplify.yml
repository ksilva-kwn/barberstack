version: 1
applications:
  - appRoot: frontend/web
    frontend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm@9
            - NODE_ENV=development pnpm install --no-frozen-lockfile
        build:
          commands:
            - pnpm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
